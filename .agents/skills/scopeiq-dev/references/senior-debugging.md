# Senior Debugging Reference

See the standalone `senior-debugging` skill for the primary workflow.

Use that skill when the task needs:

- exact reproduction steps
- fault isolation across layers
- root-cause statements tied to broken invariants
- regression tests and verification evidence

