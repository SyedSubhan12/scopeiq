[allowlist]
description = "Test placeholder keys and CI fixture values"
regexes = [
  "sk_test_placeholder",
  "re_test_placeholder",
  "whsec_placeholder",
  "generate_a_secure_random_secret_here",
  "your_gemini_api_key_here",
]

# Allow .env.example — it intentionally contains placeholder values
paths = [
  ".env.example",
]
